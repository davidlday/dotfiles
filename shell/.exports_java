# Java
if [ is-macos ]; then
  is-executable "/usr/libexec/java_home" && export JAVA_HOME=$(/usr/libexec/java_home)
else
  export JAVA_HOME=$(dirname $(dirname $(readlink -f $(which java))))
fi

if [ -z ${JAVA_HOME+x} ]; then
  prepend-path "${JAVA_HOME}/bin"
fi
